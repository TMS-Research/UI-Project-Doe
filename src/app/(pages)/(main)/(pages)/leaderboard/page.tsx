import { Leaderboard } from "@/app/(pages)/(main)/(pages)/leaderboard/components/leaderboard";

export default function LeaderboardPage() {
  return (
    <main className="container mx-auto py-8">
      <Leaderboard />
    </main>
  );
}
